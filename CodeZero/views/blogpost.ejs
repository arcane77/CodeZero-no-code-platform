<% %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://i.postimg.cc/ZYHGW3z0/logo192.png" rel="icon">
    <link href="https://i.postimg.cc/ZYHGW3z0/logo192.png" rel="apple-touch-icon">
    <title>Blog Post</title>
</head>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@200;400&display=swap');
body {
    display: flex;
    flex-direction: column;
    /* align-items: center; */
    height: 100vh;
    background-color: #f5f5f5;
    font-family: 'Montserrat', sans-serif;
}

.parent{
    display: flex;
    
}

.img{
    margin-top: 4px;
}

.text-1{
    font-weight: 1000;
    font-size: 42px;
    background: -webkit-linear-gradient(43deg,#4158D0 0%, #C850C0 46%, #FFCC70 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    max-width: 37rem;
    display: inline-flex;
    margin: 0.7rem 1rem;
    word-spacing: 0.2rem;
    letter-spacing: 0.1rem;
}

.text-2{
    font-weight: 800;
    font-size: 24px;
    background: -webkit-linear-gradient(43deg,#4158D0 0%, #C850C0 46%, #FFCC70 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-left: 5.5rem;
    
}

.form-container {
    margin-top: 3rem;
    padding: 20px;
    position: absolute;
    top: 15%;
    left:28%;
    width: 100%;
    max-width: 700px;
}

.flex-container {
            display: flex;
            justify-content: space-between;
            align-items: center;

        }

        
        .image-upload-container {
            margin-left: 300px;
            margin-bottom: 2.9rem;
        }


label {
    display: block;
    margin-bottom: 12px;
    font-weight: 900;
    font-size: 18px;
}

#title, #content, #template, #keywords, #description {
    width: 70%;
    padding: 20px;
    border: none;
    margin-bottom: 2rem;
    background: #e0e0e0;
  box-shadow: 15px 7px 30px #bebebe,
             -15px -7px 30px #ffffff;
    border-radius: 10px;
}



.upload{
    background-image: -webkit-linear-gradient(43deg,#bf79d8 0%, #9870a6 46%, #bf70ff 100%);
    color: white;
    width: 15%;
    font-size: 15px;
    font-weight: bold;
    padding: 5px 5px;
    margin-left: 4px;
    margin-bottom: 1rem;
    border: none;
    border-radius: 10px;
    cursor: pointer;
}

.upload-1{
    background-image: -webkit-linear-gradient(43deg,#bf79d8 0%, #9870a6 46%, #bf70ff 100%);
    color: white;
    width: 30%;
    font-size: 15px;
    font-weight: bold;
    padding: 8px 5px;
    margin-left: 4px;
    margin-top: 0.5rem;
    border: none;
    border-radius: 10px;
    cursor: pointer;
}

.btn {
    background-image: -webkit-linear-gradient(43deg,#4158D0 0%, #C850C0 46%, #FFCC70 100%);
    color: white;
    width: 35%;
    font-size: 15px;
    font-weight: bold;
    padding: 10px 15px;
    margin-left: 9rem;
    border: none;
    border-radius: 15px;
    cursor: pointer;
}

.circle-btn {
    width: 50px;
    height: 50px;
    margin: 11px 0 0 10px;
    background-color: white;
    border: none;
    border-radius: 50%; 
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); 
    cursor: pointer;
    outline: none;
    position: relative;
    transition: transform 0.3s, box-shadow 0.3s;
}

.circle-btn:hover {
    transform: translateX(-2px); 
    box-shadow: 0px 6px 8px rgba(0, 0, 0, 0.1); 
}

.circle-btn::before {
    content: ""; 
    position: absolute;
    width: 15px;
    height: 15px;
    background-color: #7c3cb8; 
    clip-path: polygon(100% 50%, 0% 0%, 0% 100%); 
    transform: rotate(181deg);
    top: 45%;
    left: 45%;
    margin-left: -7px; 
    margin-top: -5px; 
}


.project-list {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px;
  margin-bottom: 10px;
  margin-left: -2rem;
  padding: 0;
}


.project-item {
    position: relative;
    cursor: pointer;
    transition: transform 0.25s ease;
}

.project-item input[type="radio"] {
    position: absolute;
    opacity: 0;
    cursor: pointer;
}

.project-item:hover .project-img::before {
    background: hsla(0, 0%, 0%, 0.5);
}

.project-item:hover img {
    transform: scale(1.1);
}

.project-item:hover {
    transform: scale(1.05);
}

.project-item input[type="radio"]:checked + .project-img::before {
    background: hsla(0, 0%, 0%, 0.3);
}

.project-item input[type="radio"]:checked + .project-img img {
    transform: scale(1.05);
}


.project-img {
  position: relative;
  width: 100%;
  height: 200px;
  border-radius: 16px;
  overflow: hidden;
  margin-bottom: 15px;
}

.project-img::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: transparent;
  z-index: 1;
  transition: 0.25s ease;
}


.project-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition:0.25s ease;
}

.project-title
{ 
    margin-left: 45px; 
}

.project-title {
  color: hsl(0, 0%, 5%);
  font-size: 16px;
  font-weight: 500;
  text-transform: capitalize;
  line-height: 1.3;
  text-decoration: none;
}

.temp-container {
    margin-right: 10rem;
    padding-right: 2rem;

}

.social {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
}

.input-group { 
    margin-right: 3rem;
}

.text-area {
    min-width: 95%; 
    height: auto; 
    min-height: 25px; 
}

.input-placeholder {
    position: absolute;
    bottom: 28rem;
    right: 28rem;
    color: grey;
    pointer-events: none;
    font-size: 0.7em;
    display: block;
  }
    
</style>

<body>
    <div class="parent">
        <a href="landing"><button class="circle-btn"></button></a>
        <p class="text-1">Blog Post</p>
    </div>
    <p class="text-2">Describe your Blog Post!</p>

<form action="/saveblog" method="POST" enctype="multipart/form-data">
    <div class="form-container">
        <div class="flex-container">
            <div class="title-container">
                <label>Blog Title</label>
                <input type="text" id="title" style="width: 210%;" name="title" required>
            </div>

            <div class="image-upload-container">
                <label for="upload-file-bg">Add Background Image</label>
                <button id="trigger-upload-bg" class="upload-1">Add</button>
                <input type="file" id="upload-file-bg" name="image" accept="image/*" style="display: none;" required>
            </div>
        </div>

        <label>Blog Content</label>
        <textarea id="content" name="content" rows="9" cols="40" required></textarea>

        <div class="social">
            <div class="input-group">
                <label for="description">Short Description</label>
                <textarea id="description" name="description" class="text-area" placeholder="enter short description of the blog post" cols="25" rows="5"  oninput="limitCharacters()" required></textarea>
                <span class="input-placeholder" id="char-count">0 / 100</span>
            </div>

            <div class="input-group">
                <label for="keywords">Keywords (comma-separated)</label>
                <input type="text" id="keywords" name="keywords" placeholder="enter keywords" required>
            </div>
        </div>

        <label style="margin-top: 1rem;">Select Template</label>
        <div class="temp-container">
            <ul class="project-list">

                <!-- optional  for loop -->
                <label class="project-item active">
                <input type="radio" name="template_id" id="template1" value="1" required>
                <figure class="project-img">
                    <img src="images/verical.png" alt="vertical" loading="lazy">
                </figure>
                <h3 class="project-title">Vertical Blog Post</h3>
                </label>

                <label class="project-item active">
                <input type="radio" name="template_id" id="template2" value="2" required>
                <figure class="project-img">
                    
                    <img src="images/horizon.png" alt="horizon" loading="lazy">
                </figure>
                <h3 class="project-title">Horizontal Blog Post</h3>
                </label>
                <!--  -->

            </ul>
        </div>

        <button class="btn" type="submit">Generate!</button>
    </div>
</form>

<script>
    document.getElementById('trigger-upload-bg').addEventListener('click', function(event) {
        event.preventDefault();
        document.getElementById('upload-file-bg').click();
    });

    document.getElementById('upload-file-bg').addEventListener('change', function() {
        if (this.value) {
            alert('Background file selected!');
        }
    });

     //limit and update count
 function limitCharacters() {
    var input = document.getElementById('description');
    var charCount = document.getElementById('char-count');
    var maxLength = 100;

    //update count
    charCount.textContent = input.value.length + " / " + maxLength;

    //trim val if exceeds
    if (input.value.length > maxLength) {
      input.value = input.value.substring(0, maxLength);
    }
  }

  document.addEventListener('DOMContentLoaded', function() {
  limitCharacters(); //initialize count
});

</script>

</body>
</html>